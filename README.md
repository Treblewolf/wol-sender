# wol-sender
A lightweight Wake-on-LAN (WOL) utility that supports Win95 till Win11, written in pure C using the native Win32 API. It allows you to manage a list of computers, check their online status via ICMP or TCP, and wake them up using Magic Packets.
